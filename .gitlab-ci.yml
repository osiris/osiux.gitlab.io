image: os1ux/alpine-org2blog

before_script:
  - awk --version
  - emacs --version
  - pandoc --version
  - md2gemini --version

pages:
  script:
  - make requirements
  - make blog
  - make stats_posts
  - make stats_since_until
  - make index
  - make years
  - make image
  - make links
  - make markdown
  - make gemini
  - make publish
  - make sitemap
  - make htaccess
  - make tar_gz
  artifacts:
    paths:
    - public
  only:
  - master

image: iquiw/alpine-emacs

before_script:
  - apk update && apk add bash gawk imagemagick curl make
  - awk --version
  - emacs --version

pages:
  script:
  - make requirements
  - make index
  - make blog
  - make stats_posts
  - make years
  - make image
  - make links
  - make publish
  - make sitemap
  - make htaccess
  - make tar_gz
  artifacts:
    paths:
    - public
  only:
  - master
